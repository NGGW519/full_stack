# 터미널로 docker의 postgre 이미지 설치하기
docker run --name my-postgres2 -e POSTGRES_USER=nggw -e POSTGRES_PASSWORD=1111 -e POSTGRES_DB=mydb -p 15432:5432 -d postgres:16

위 내용을 한줄로 쓰면됨
그 후에 datagrip으로 postgres로 접근할때 위에서 적은 내용을 그대로 적으면된다.

이미지 삭제할때,
컨테이너 돌고있는지 확인 > 컨테이너 중지 > 중지된 컨테이너 삭제 > 이미지 삭제
위의 순서대로 해야한다.